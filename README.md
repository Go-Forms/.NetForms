# NetForms

Cross-platform (Windows + Linux) implementation of Windows Forms for .NET —
same `System.Windows.Forms` API, same `Program.cs` / `Form.cs` / `Form.Designer.cs`
workflow, rendered identically everywhere on top of Avalonia + SkiaSharp.

See `docs/PLAN.md` for the architecture and roadmap. Work in progress.
