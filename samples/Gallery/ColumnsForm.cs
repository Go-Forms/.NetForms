namespace Gallery
{
    /// <summary>
    /// Columns as the designer writes them - each a component with a field and a block of its own:
    /// DataGridView columns and ListView column headers.
    /// </summary>
    public partial class ColumnsForm : Form
    {
        public ColumnsForm()
        {
            InitializeComponent();
        }
    }
}
