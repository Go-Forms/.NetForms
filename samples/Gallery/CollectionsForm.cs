namespace Gallery
{
    /// <summary>
    /// Collections the designer writes through local variables, as Visual Studio does: tree nodes,
    /// list view groups and items, a data grid's cell style.
    /// </summary>
    public partial class CollectionsForm : Form
    {
        public CollectionsForm()
        {
            InitializeComponent();
        }
    }
}
